# utila-music-bot configuration

# Required
DISCORD_TOKEN=YOUR_BOT_TOKEN_HERE

# Optional: comma-separated guild IDs to register commands instantly during development.
# Leave blank to register commands globally (may take up to 1 hour to propagate).
DEV_GUILDS=123456789012345678,987654321098765432

# Optional owner IDs (comma separated). Owners bypass channel checks for controls.
OWNER_IDS=123456789012345678

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Disconnect after N minutes of inactivity (no queue and not playing)
IDLE_DISCONNECT_MINUTES=5
